{% extends '@!EasyAdmin/layout.html.twig' %}

{# Let\'s remove/empty the header #}
{% block content_header_wrapper %} {% endblock content_header_wrapper %}

{# The main page content block #}
{% block main %}
    <section class="content-header">
        <div class="content-header-title">
            <h1 class="title">Favoris</h1>
        </div>

{#        <div class="page-actions">#}
{#            <div class="global-actions">#}
{#                <a class=" action-new btn btn-primary" href="#" data-action-name="new"><span class="action-label">TodoBTN</span></a>#}
{#            </div>#}
{#        </div>#}
    </section>

    <section class="content">
        <div class="row">
            <div class="col-md-12">
                <div class="box box-primary">
                    <div class="box-body">
                        <div class="row">
                            <div class="col-md-12">
                                <div class="accordion accordion-flush" id="accordionFlushExample">
                                    {% for user in users %}
                                        <div class="accordion-item">
                                            <h2 class="accordion-header" id="flush-headingOne">
                                                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#flush-collapseOne" aria-expanded="false" aria-controls="flush-collapseOne">
                                                    #{{ user.id }}  -  {{ user.fullName }} ({{ user.favorites|length }} bien{{ user.favorites|length > 1 ? 's' }} en favori)
                                                </button>
                                            </h2>
                                            <div id="flush-collapseOne" class="accordion-collapse collapse" aria-labelledby="flush-headingOne" data-bs-parent="#accordionFlushExample">
                                                <div class="accordion-body">
                                                    <p>{{ user.email }}</p>
                                                    {% if user.phone %}
                                                        <p>{{ user.phone }}</p>
                                                    {% endif %}

                                                    <div class="table-responsive">
                                                        <table class="table table-striped table-bordered table-hover">
                                                            <thead>
                                                            <tr>
                                                                <th>REF</th>
                                                                <th>Titre</th>
                                                                <th>Type</th>
                                                                <th>Catégorie</th>
                                                                <th>Localisation</th>
                                                                <th>Action</th>
                                                            </tr>
                                                            </thead>
                                                            <tbody>
                                                            {% for favory in user.favorites %}
                                                                <tr>
                                                                    <td>{{ favory.reference }}</td>
                                                                    <td>{{ favory.title }}</td>
                                                                    <td>{{ favory.type }}</td>
                                                                    <td>{{ favory.category.name }}</td>
                                                                    <td>{{ favory.location }}</td>
                                                                    <td>
                                                                        <a href="{{ ea_url().setAction('detail').setEntityId(favory.id).setController('App\\Controller\\Admin\\PropertyCrudController') }}" class="btn btn-primary btn-sm">Consulter</a>
                                                                    </td>
                                                                </tr>
                                                            {% endfor %}
                                                            </tbody>
                                                        </table>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    {% endfor %}
                                </div>
{#                                <div class="table-responsive">#}
{#                                    <table class="table table-striped table-bordered table-hover">#}
{#                                        <thead>#}
{#                                            <tr>#}
{#                                                <th>Id</th>#}
{#                                                <th>Client</th>#}
{#                                                <th>Nombre de favoris</th>#}
{#                                                <th>Actions</th>#}
{#                                            </tr>#}
{#                                        </thead>#}
{#                                        <tbody>#}
{#                                            {% for user in users %}#}
{#                                                <tr>#}
{#                                                    <td>{{ user.id }}</td>#}
{#                                                    <td>{{ user.fullName }}</td>#}
{#                                                    <td>{{ user.favorites|length }}</td>#}
{#                                                    <td>#}
{#                                                        <a href="{{ path('admin_dashboard', {'routeName': 'app_favory_showe', 'menuIndex': 5, 'id': user.id}) }}" class="btn btn-primary btn-xs">#}
{#                                                            Consulter#}
{#                                                        </a>#}
{#                                                    </td>#}
{#                                                </tr>#}
{#                                            {% endfor %}#}
{#                                        </tbody>#}
{#                                    </table>#}
{#                                </div>#}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock main %}

{# Let\'s remove/empty the footer #}
{% block content_footer_wrapper %} {% endblock content_footer_wrapper %}